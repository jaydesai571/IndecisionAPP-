console.log('Utils.js is running');

// const sqaure = (x) => x*x;

// const add = (a,b) => a+b;

// export { sqaure, add };

export const isAdult = (x) => x >= 18;

export const canDrink = (x) => x >= 19;