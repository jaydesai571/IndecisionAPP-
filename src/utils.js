console.log('Utils.js is running');

// const sqaure = (x) => x*x;
// const add = (a,b) => a+b;
// const sub = (a,b) => a-b;
// export { sqaure, add, sub as default };

export const isAdult = (x) => x >= 18;

export const canDrink = (x) => x >= 19;

export  default (x) => x >= 65; 